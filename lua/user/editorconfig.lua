local function setup()
  -- local editorconfig = require("editorconfig")
  -- editorconfig.properties.spell_lang = function(bufnr, val)
  --   vim.bo[bufnr].spelllang = val
  -- end
end

return {
  setup = setup,
}
